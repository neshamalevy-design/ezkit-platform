# Design — פלטפורמת EZ-kit (שלד ראשוני)

נכתב במסגרת מודול תרגול הסיום של אִימפְּרוּב.
שלב 8 מתוך 11. תאריך: 2026-05-28

מבוסס על: `spec.md` / `copy.md` / `assets/ezkit-multi-angle.png`

---

## החלטה 1: רכיב מ-21st.dev

**הרכיב:** Hero section עם punchline בולטת + 2 כפתורי CTA צמודים + תמונת הזיקית בצד.

**איפה בדף:** Homepage בלבד. בתוך הפלטפורמה (login, dashboards) — UI נקי ללא decoration.

**למה זה:** הזיקית היא mascot של EZ-kit. נוכחותה ב-Homepage יוצרת זהות ויזואלית — מה שיעזור למורות לזהות את המערכת ולחזור אליה. בתוך הפלטפורמה היא חוזרת להיות כלי עבודה, ולא צריכה decoration שמפריע.

---

## החלטה 2: פלטת צבעים — מבוססת עקרונות נגישות חושית

הפלטה נבחרה במכוון לקהל של חינוך מיוחד — צבעים פסטליים ורכים שלא מעמיסים על מערכת החושים. הקרם כרקע במקום לבן בוהק, ניגודיות מספקת אבל לא קיצונית.

| תפקיד | צבע | קוד | שימוש |
|---|---|---|---|
| **רקע** | קרם רך | `#FAF6F0` | רקע ראשי של כל הדפים |
| **טקסט** | אפור-כחול כהה | `#2D3A4A` | כל הטקסטים — כותרות ופסקאות |
| **הדגשה ראשית** | תכלת רגוע | `#7BAFD4` | כפתור CTA מורה, accents, links |
| **הדגשה משנית** | ירוק מרווה | `#8FBC9B` | אזור "החומרים שלי", indicators חיוביים |
| **התראה עדינה** | כתום אפרסק | `#E8A87C` | אזור "צרי התאמה" (פעולה ראשית), אזהרות עדינות |

**למה:** מבוסס על המלצות מקצועיות לעיצוב ממשקים לאוכלוסיית חינוך מיוחד:
- צבעים פסטליים מרגיעים, לא מעמיסים על חושים
- קרם במקום לבן בוהק — מפחית סנוור
- אפור-כחול כהה במקום שחור — ניגודיות לקריאות ללא קיצוניות שמעייפת
- הימנעות מאדום עז וצהוב זוהר
- כל אזור ב-Dashboard מקבל צבע אחר (לא רק טקסט) — נגישות לאוכלוסייה עם עיוורון צבעים או קושי בעיבוד

---

## החלטה 3: אופי כללי

**האופי:** רגוע, נגיש, מקצועי.

**איך זה מתבטא:**
- **פונט:** Rubik / Heebo / Assistant — פונט עברי קליל וברור
- **רווחים:** הרבה white space, לא לדחוס. רווחים גדולים בין סקציות
- **אנימציות:** עדינות בלבד — hover על כפתורים, מעבר רך בין דפים. בלי אנימציות שמעמיסות
- **היררכיה:** מבנה ברור בכל מסך — כותרת → תוכן → פעולה
- **כיווניות:** RTL מובהקת לאורך כל הפלטפורמה
- **עקביות:** אותם צבעים, פונטים, ופריסה בכל המסכים — כדי להעצים תחושת ביטחון ומקום

---

## דרישות נגישות (להוסיף לפרומפט של Claude Code בשלב 9)

הפלטפורמה מיועדת לאוכלוסייה רגישה — תלמידי חינוך מיוחד. דרישות נגישות מינימליות:

1. **WCAG AA** — לוודא יחס ניגודיות מינימלי 4.5:1 בין טקסט לרקע בכל הצירופים
2. **גודל פונטים** — מינימום 16px לטקסט גוף, 20px+ לכותרות. אפשרות להגדלה
3. **אזורים מובחנים בצורה ובצבע** — לא להסתמך על צבע בלבד. כל אזור Dashboard יקבל אייקון + תווית + צבע ייחודי
4. **focus states ברורים** — כשמורה/תלמיד עובר על אלמנט עם מקלדת, מסגרת ברורה
5. **alt text לכל התמונות** — כולל הזיקית

---

## הערות לשלב 9 (Build)

כשנמסור ל-Claude Code:
- multi-page app עם ראוטינג בסיסי (HTML/CSS/JS פשוט, ללא React)
- **מבנה תיקיות סופי (עודכן עם מסך "הכיתה שלי"):**

```
my-page/
├── index.html                    (Homepage)
├── styles.css                    (CSS מרכזי עם variables)
├── teacher/
│   ├── login.html
│   ├── dashboard.html            (3 הקופסאות: הכיתה שלי / החומרים שלי / צרי התאמה)
│   └── class.html                (הכיתה שלי — רשימה + הוספה + אפיון)
├── student/
│   ├── login.html
│   └── dashboard.html
└── assets/
    └── ezkit-multi-angle.png
```

- קובץ `styles.css` מרכזי עם CSS variables לצבעים שהוגדרו כאן — כך שעדכון צבע בעתיד יהיה במקום אחד
- הזיקית (`ezkit-multi-angle.png`) מופיעה רק ב-Homepage. אפשר לחתוך רק את החזית (הריבוע השמאלי) אם זה ייצא יפה יותר ויזואלית
- **מסך `class.html`:** רשימת תלמידי placeholder (3-5 דמה לתצוגה), כפתור "+ הוסף תלמיד" בולט, וסטטוסי אפיון בצורה ויזואלית (תווית עם צבע + טקסט — לא רק צבע, לפי דרישות נגישות)
