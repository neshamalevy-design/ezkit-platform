# Spec — פלטפורמת EZ-kit (שלד ראשוני)

נכתב במסגרת מודול תרגול הסיום של אִימפְּרוּב.
שלב 6 מתוך 11. תאריך: 2026-05-28
עדכון: 2026-05-28 — נוסף מסך "הכיתה שלי" + מבנה Dashboard סופי

---

## 1. מה הדף עושה?

**שלד פונקציונלי של פלטפורמת EZ-kit** — לא דף נחיתה, אלא frontend ראשוני של מערכת עבודה שעתידה לעבוד כפלטפורמה דומה לאופק של מט"ח: מסלולים נפרדים למורה ולתלמיד, מבנה ארכיטקטוני ברור, ומקום פיזי (URL פומבי) שאליו יתחבר ה-backend האמיתי בהמשך הפיתוח.

### 6 המסכים בשלד

1. **Homepage** (ציבורי) — מסך פתיחה עם hero, ה-punchline, הסבר קצר על EZ-kit, ושני כפתורי כניסה (מורה / תלמיד). הזיקית של EZ-kit מופיעה כ-mascot.
2. **Login מורה** — טופס כניסה (מייל + סיסמה). UI בלבד, לא פונקציונלי.
3. **Login תלמיד** — טופס כניסה (קוד תלמיד / שם משתמש). UI בלבד, לא פונקציונלי.
4. **Dashboard מורה** — 3 קופסאות עיקריות שמובילות לאזורי העבודה.
5. **הכיתה שלי** (מסך פנימי, נגיש מ-Dashboard המורה) — רשימת תלמידים + כפתור הוספת תלמיד + לחיצה על תלמיד מובילה לאפיון.
6. **Dashboard תלמיד** — מסך ריק עם placeholders: "דפי העבודה שלי", "משימות פעילות".

### מבנה Dashboard מורה — 3 קופסאות

| מס' | קופסה | מה היא עושה |
|---|---|---|
| 1 | **הכיתה שלי** | רשימת תלמידים + הוספה + אפיון. הקופסה הראשונה שמורה נכנסת אליה |
| 2 | **החומרים שלי** | מאגר דפי עבודה שהמורה העלתה |
| 3 | **צרי התאמה לתלמיד** | הפעולה המרכזית — בוחרים טקסט + תלמיד = גרסה מותאמת |

הסדר משקף את **זרימת העבודה הטבעית: תלמיד → חומר → התאמה.**

### עיקרון מנחה

> "כרגע לא חשוב מה יראו מאחורי כל דבר. חשוב שיהיה מקום לקשר אליו את מה שאבנה מאוחר יותר."
> — נחמה, מתוך שיחת ה-Spec

הסקופ הוא **תשתית**, לא מוצר מוגמר. ראוטינג עובד, מבנה תיקיות מסודר ב-Claude Code, ומקום ברור לכל אזור פונקציונלי עתידי.

---

## 2. למי הדף הזה?

לא קהל רחב חיצוני. שלוש קבוצות:

- **נחמה עצמה** — כדי לראות איך הפלטפורמה תיראה לפני שמשקיעים בקוד אמיתי
- **מורות הפיילוט שכבר ידועות** — שיוכלו לראות מראש את חוויית העבודה ולתת פידבק על UX לפני שזה נכתב בקוד
- **בעתיד** — מתכנתים שיעבדו על ה-backend, יקבלו מפת דרכים ויזואלית של מה לבנות

---

## 3. המסר המרכזי

> **"להתאים את הדף לתלמיד, לא את התלמיד לדף."**

יופיע כ-hero ב-Homepage. בתוך הפלטפורמה — אין צורך במסר שיווקי, רק UI פונקציונלי.

---

## 4. הרגש

**כבוד והערכה.**

- כבוד לתלמיד — לא מנסים לשנות אותו, מתאימים לו את הסביבה
- כבוד למורה — נותנים לה כלי רציני שמכבד את הזמן והשיקול שלה
- כבוד לפרויקט — מערכת עבודה אמיתית, לא demo "שווה"

ביטוי בעיצוב: נקי, מאופק, רציני. לא צעקני, לא דרמטי. בגובה העיניים.

---

## 5. ה-CTA

ב-Homepage: **שני כפתורים מקבילים** — "כניסת מורה" + "כניסת תלמיד".

בתוך הפלטפורמה: כל מסך מוביל לפעולה הבאה בתהליך העתידי (אפיין תלמיד → העלה טקסט → צור התאמה וכו'). היום זה רק placeholders — מחר יהפכו לכפתורים פעילים.

---

## הערות לשלב 9 (Build)

כשנמסור ל-Claude Code לבנות:
- מבנה תיקיות מסודר שיתאים להוספת backend בעתיד
- ראוטינג ב-vanilla JavaScript (פשוט, ללא framework מורכב — לא נצרך עכשיו)
- HTML/CSS נקי, components נפרדים לכל מסך
- placeholders שמסומנים בבירור כ"כאן יבוא X" — לא להציג תוכן מומצא

**מבנה קבצים מוצע:**

```
my-page/
├── index.html                    (Homepage)
├── styles.css                    (CSS מרכזי עם variables)
├── teacher/
│   ├── login.html
│   ├── dashboard.html            (3 הקופסאות)
│   └── class.html                (הכיתה שלי — רשימה + הוספה + אפיון)
├── student/
│   ├── login.html
│   └── dashboard.html
└── assets/
    └── ezkit-multi-angle.png
```
